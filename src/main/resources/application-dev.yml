server:
  port: 8082

# Local Mongodb database (local or Dockerized) -------------
spring:
  data:
    mongodb:
      uri: mongodb://mongo:mongo@localhost:27017/tpvarticle?authSource=admin

springdoc:
  swagger-ui:
    oauth:
      client-id: client-id
      client-secret: client-secret
